const defaults = {
  strings: [
    "hey lovely,",
    "use your own string...",
    "abi you no go use am"
  ],
  loadspeed: 9,
  char: "-",
  hideChaar: true,
  delay: 2500,
  loop: true,
  randomizeEl: true,
};

export default defaults;
